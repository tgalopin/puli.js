'use strict';

/**
 * @todo
 */
export default class Puli {
    constructor(configFile) {
        this.pathMappings = configFile;
    }

    path(puliPath) {
        let filesystemPath = '';

        // Resolve ...

        return filesystemPath;
    }
}
